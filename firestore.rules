rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow all authenticated users to access all collections
    // This is a simplified approach to avoid permission issues
    match /{document=**} {
      allow read, write, create, update, delete: if request.auth != null;
    }
  }
}